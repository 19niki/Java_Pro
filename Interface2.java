/*
  Even if we don't add the abstract keyword to the Inteface by default it make it as abstarct method. 
  But in abstract we must need to give the keyword abstarct or else we will get CTE

  abstract Pro
  {
	  void add(); // CTE
  }
*/


abstract interface Interface2
{
	void add();
} 
